services:
  redis:
    build: ./redis  # Points to folder with Dockerfile + redis.conf
    container_name: redis-development
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  redis_data:

#docker compose build --build-arg  REDIS_PASSWORD=
#docker compose up -d